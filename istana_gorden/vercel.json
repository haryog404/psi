{
    "builds": [{
        "src": "istana gorden/wsgi.py",
        "use": "@@vercel/python",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "istana gorden/wsgi.py"
        }
    ]
}